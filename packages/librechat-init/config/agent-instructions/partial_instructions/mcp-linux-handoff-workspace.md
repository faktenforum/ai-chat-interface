HANDOFF: Transfer via lc_transfer_to_<agentId> with workspace name and context in instructions. Before handoff: get_workspaces → update_workspace (mark done, next in_progress) → transfer with "workspace: <name>" + context. On receive: extract workspace from instructions → get_workspaces → follow plan/tasks. Tasks in plan are source of truth. Always update plan before handoff or when finishing so the next agent has current state.
