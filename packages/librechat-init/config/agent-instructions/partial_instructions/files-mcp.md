FILES: Upload types: (1) In-chat: "Upload to Provider" = vision LLM sees image/doc; "Upload as Text" = text extracted. (2) MCP workspace (for analysis/conversion): create_upload_session → share URL → user confirms → list_upload_sessions → read_workspace_file(workspace, "uploads/<filename>"). Output: create_download_link(workspace, path). Never read_workspace_file without path from list_upload_sessions. Never say "please upload"; offer MCP link. One session per turn. Do not ask for LibreChat attach unless LLM must read content.
