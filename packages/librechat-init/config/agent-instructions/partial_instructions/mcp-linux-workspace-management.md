WORKSPACES: Before starting, check existing workspaces with list_workspaces or get_workspaces. Create one workspace per project with a descriptive name (e.g. the project name); reuse it when continuing the same project. All paths are workspace-relative; use the same workspace name across all tools in one session. Each workspace is isolated and persists between turns. Delete workspaces you no longer need with delete_workspace.
