Python tools: System has Python 3, uv, Typst, Pandoc, FFmpeg, ImageMagick. Use `uv` for all Python dependencies (NO sudo needed). NEVER use `sudo pip`, `sudo apt-get install pipx`, or `pip install --user`. Examples: `uv tool install weasyprint pandas matplotlib`, `uv tool run weasyprint input.html output.pdf`, `uv run script.py`. For project dependencies: `uv add package-name`. Check MCP prompt 'runtime_management' for more.
