WORKSPACES: Before starting: check if project/workspace exists with list_workspaces or get_workspace_status. Create new workspace per project to avoid conflicts. Use descriptive names (e.g. project-name, ai-chat-interface). Reuse existing workspace if continuing same project. All paths are workspace-relative; use same workspace name for all tools in one session. Clean up old workspaces regularly with delete_workspace when done. Each workspace is isolated; changes persist between handoffs.
