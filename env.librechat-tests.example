# LibreChat test stack – used only by docker-compose.librechat-tests.yml.
# Copy to .env.librechat-tests before first run:
#   cp env.librechat-tests.example .env.librechat-tests

# Meilisearch master key (required for the meilisearch service in this stack).
# Purpose: Authenticates Meilisearch in the LibreChat test stack.
# Format: Any non-empty string; use a fixed test-only value for local runs.
# Optionality: Required when starting the stack.
MEILI_MASTER_KEY=change-me-test-meili-key
